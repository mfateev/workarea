# Repository storage - shared across all workspaces
/repos/

# Workspaces - all workspace content is local/gitignored
# Only .gitkeep is tracked to preserve the directory structure
/workspaces/*
!/workspaces/.gitkeep

# Legacy paths - keep during migration transition
# These patterns handle the old structure at root level
# Can be removed after confirming migration is complete
tasks/*/*
!tasks/*/*.md
!tasks/*/*.json

# Temporary files
*.swp
*.swo
*~
.DS_Store

# IDE files
.idea/
.vscode/
*.iml

# Build artifacts
*.log
.tmp/
